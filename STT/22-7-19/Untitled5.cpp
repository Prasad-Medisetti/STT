#include<stdio.h>
int main()
{
	int a[100]={0},i,*p,n,k,r;
	printf("Enter size:");
	scanf("%d",&n);
	printf("\nEnter array elements:");
	for(i=0;i<n;i++)
	scanf("%d",&a[i]);
	printf("\nEnter number of rotations need to perform:");
	scanf("%d",&k);
	r=k%n;
	while(i<j)
	{
		for(i=0;i<n;i++)
		{
			printf("\n%p",p);
			p++;
		}
	}
	return 0;
}
